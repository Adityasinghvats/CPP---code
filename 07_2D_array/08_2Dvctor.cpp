#include<iostream>
#include<vector>
using namespace std;
int main(){
    vector<vector<int>> matrix;
    int row = matrix.size();
    // 2D array is a array of arrays
    int column = matrix[0].size();
    vector<vector<int>> transpse(row,vector<int>(column));
}