#include<bits/stdc++.h>
using namespace std;
int main(){
    // In descending order
    sort(v.begin(),v.end(),greater<int>);

    // Custom sorting comparator
    bool comp(pair<int,int> p1, pair<int,int>p2){
        
    }
    sort(a,a+n,comp);
}